import { ImageResponse } from "next/og";
import { postsRepository } from "@/lib/repositories/posts";
import { settingsRepository } from "@/lib/repositories/settings";
export const size={width:1200,height:630}; export const contentType="image/png";
export default async function Image({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const [post,settings]=await Promise.all([postsRepository.getPublishedBySlug(slug),settingsRepository.getPublic()]);const title=post?.title||settings.siteName;const category=post?.categoryName||post?.category||"مقال";return new ImageResponse(<div dir="rtl" style={{width:"100%",height:"100%",display:"flex",flexDirection:"column",justifyContent:"space-between",padding:"72px",background:"#f7f7f5",color:"#171717",fontFamily:"sans-serif",textAlign:"right"}}><div style={{display:"flex",justifyContent:"space-between",fontSize:30,fontWeight:700}}><span>{settings.siteName}</span><span style={{color:"#5b5b5b"}}>{category}</span></div><div style={{display:"flex",fontSize:title.length>90?48:title.length>55?58:68,fontWeight:800,lineHeight:1.45,maxWidth:"1060px"}}>{title}</div><div style={{display:"flex",fontSize:26,color:"#5b5b5b"}}>{post?.author?.name||settings.authorName}</div></div>,size);}
